# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Progress {
  target: Target!
  trainer: Trainer!
}

type Target {
  id: Int!
  challenge: Challenge!
  pkmn: PKMN!
  progress: [Progress!]!
}

type PKMN {
  id: Int!
  dexNumber: Int!
  name: String!
  variance: String
  variantId: String
  variantName: String
  spriteURL: String!
}

type Challenge {
  id: Int!
  name: String!
  targets: [Target!]!
}

type Trainer {
  id: Int!
  name: String!
  password: String!
  targets: [Progress!]
}

type Query {
  getProgress(challengeId: Int!): [Progress!]!
  getAllPKMN: [PKMN!]!
  getChallenges: [Challenge!]!
  getChallenge(id: Int!): Challenge
  getTargets(challengeId: Int!): [Target!]!
  getTarget(id: Int!): Target
}

type Mutation {
  signUp(username: String!, password: String!): String!
  logIn(username: String!, password: String!): String!
}